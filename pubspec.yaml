name: window_toolbox
description: "Fully custom windows and utilities for Flutter multi-window."
version: 0.0.1
url: https://github.com/knopp/window_toolbox

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  native_toolchain_ninja: ^0.1.1
  hooks: ^1.0.2
  code_assets: ^1.0.0
  logging: ^1.3.0
  ffi: ^2.2.0
  win32: ^6.0.0
  headless_widgets: ^0.0.7

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
