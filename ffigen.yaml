# Run with `dart run ffigen --config ffigen.yaml`.
name: WindowToolboxBindings
description: |
  Bindings for `src/window_toolbox.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/window_toolbox_bindings_generated.dart'
headers:
  entry-points:
    - 'src/window_toolbox.h'
  include-directives:
    - 'src/window_toolbox.h'
ffi-native:
comments:
  style: any
  length: full
