# Run with `dart run ffigen --config ffigen.yaml`.
name: WindowkitBindings
description: |
  Bindings for `src/windowkit.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/windowkit_bindings_generated.dart'
headers:
  entry-points:
    - 'src/windowkit.h'
  include-directives:
    - 'src/windowkit.h'
ffi-native:
comments:
  style: any
  length: full
